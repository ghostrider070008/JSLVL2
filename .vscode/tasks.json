// Sass configuration
{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "gulp",
            "task": "sass",
            //"command": "node-sass css/styles.scss css/styles.css",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
} 